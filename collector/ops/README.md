# Windows operation templates

These are inert templates. Follow
`docs/telemetry/slice-18-5c-windows-runbook.md`; never render credentials in the
repository. WinSW is preferred. The Scheduled Task is a boot-only fallback,
not a periodic process launcher.
